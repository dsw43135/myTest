name: Automatyczne testowanie - Windows

on:
  schedule:
    - cron: "0 0 * * 0"
  push:
    branches:
      - master
  workflow_dispatch:
  
jobs:
  build_and_test:
    runs-on: windows-latest
    steps:
        -run: node --version

